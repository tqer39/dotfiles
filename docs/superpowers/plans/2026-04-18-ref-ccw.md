# ref-ccw Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `ccw <topic>` で Claude Code を必ず新規 git worktree + auto mode で起動し、worktree 外での編集系ツール呼び出しは `PreToolUse` hook で deny する。

**Architecture:** `src/.shell_common` の `ccw` と `src/.claude/hooks/require-worktree.sh` を symlink で配布する。`src/.claude/settings.personal.json` に `PreToolUse` を追加する。managed settings は使わず user スコープ設定で運用する。

**Tech Stack:** bash (shellcheck 対応), jq, Claude Code 2.1.113+ (`--worktree` / `--permission-mode` / hooks)

Spec: `docs/superpowers/specs/2026-04-18-ref-ccw-design.md`

---

## File Structure

- Create: `src/.claude/hooks/require-worktree.sh` — PreToolUse hook (20 行程度、単一責務: cwd 判定 + deny JSON 生成)
- Create: `scripts/tests/test-require-worktree.sh` — hook のユニットテスト（canned stdin + 期待出力アサート）
- Modify: `src/.shell_common:95-110` — `ccw` 関数定義の置換
- Modify: `src/.claude/settings.personal.json:30-41` — `hooks` ブロックに `PreToolUse` を追加
- Modify: `config/platform-files.conf` — hook の symlink エントリ追加
- Modify: `CLAUDE.md` — Worktree Workflow セクション追加

ファイル境界の方針:

- `ccw` 関数はシェル側の起動ラッパーに徹する（Claude Code に渡す prompt と起動フラグ整形のみ）
- `require-worktree.sh` は hook 入出力プロトコルのみに責務を限定（cwd チェック + deny JSON）
- テストは `scripts/tests/` 配下に分離、実装と独立して追加/実行可能

---

## Task 1: hook テスト作成（失敗状態）

**Files:**

- Create: `scripts/tests/test-require-worktree.sh`

hook 実装前に、期待挙動を検証する shell テストスクリプトを先に書く（TDD）。

- [ ] **Step 1: テストスクリプトを作成**

```bash
mkdir -p scripts/tests
```

ファイル内容 `scripts/tests/test-require-worktree.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

# Usage: scripts/tests/test-require-worktree.sh
# Tests src/.claude/hooks/require-worktree.sh via canned stdin JSON.

readonly HOOK="$(cd "$(dirname "$0")/../.." && pwd)/src/.claude/hooks/require-worktree.sh"

if [[ ! -x "$HOOK" ]]; then
  echo "FAIL: hook script not executable at $HOOK" >&2
  exit 1
fi

fail_count=0

assert_allow() {
  local label="$1" input="$2"
  local output rc
  output="$(printf '%s' "$input" | "$HOOK" 2>&1)" && rc=0 || rc=$?
  if [[ $rc -ne 0 ]]; then
    echo "FAIL [$label]: expected exit 0, got $rc (output: $output)" >&2
    fail_count=$((fail_count + 1))
    return
  fi
  if [[ -n "$output" ]]; then
    echo "FAIL [$label]: expected empty output, got: $output" >&2
    fail_count=$((fail_count + 1))
    return
  fi
  echo "PASS [$label]"
}

assert_deny() {
  local label="$1" input="$2"
  local output rc decision
  output="$(printf '%s' "$input" | "$HOOK")" && rc=0 || rc=$?
  if [[ $rc -ne 0 ]]; then
    echo "FAIL [$label]: expected exit 0, got $rc" >&2
    fail_count=$((fail_count + 1))
    return
  fi
  decision="$(printf '%s' "$output" | jq -r '.hookSpecificOutput.permissionDecision // empty')"
  if [[ "$decision" != "deny" ]]; then
    echo "FAIL [$label]: expected permissionDecision=deny, got: $decision (output: $output)" >&2
    fail_count=$((fail_count + 1))
    return
  fi
  echo "PASS [$label]"
}

# Case 1: cwd inside worktree + Edit → allow
assert_allow "inside-worktree-edit" \
  '{"cwd":"/Users/me/project/.worktrees/foo","tool_name":"Edit"}'

# Case 2: cwd outside worktree + Edit → deny
assert_deny "outside-edit" \
  '{"cwd":"/Users/me/project","tool_name":"Edit"}'

# Case 3: cwd outside worktree + Write → deny
assert_deny "outside-write" \
  '{"cwd":"/Users/me/project","tool_name":"Write"}'

# Case 4: cwd outside worktree + NotebookEdit → deny
assert_deny "outside-notebook-edit" \
  '{"cwd":"/Users/me/project","tool_name":"NotebookEdit"}'

# Case 5: cwd outside worktree + Bash → allow (Bash は素通し)
assert_allow "outside-bash" \
  '{"cwd":"/Users/me/project","tool_name":"Bash"}'

# Case 6: cwd outside worktree + Read → allow
assert_allow "outside-read" \
  '{"cwd":"/Users/me/project","tool_name":"Read"}'

if [[ $fail_count -gt 0 ]]; then
  echo "$fail_count test(s) failed" >&2
  exit 1
fi
echo "All tests passed"
```

- [ ] **Step 2: 実行権限を付与**

```bash
chmod +x scripts/tests/test-require-worktree.sh
```

- [ ] **Step 3: 失敗確認**

Run: `scripts/tests/test-require-worktree.sh`
Expected: exit 1。エラーメッセージに `FAIL: hook script not executable` を含む（hook 未作成のため）。

- [ ] **Step 4: コミット**

```bash
git add scripts/tests/test-require-worktree.sh
git commit -m "$(cat <<'EOF'
🧪 require-worktree hook の失敗テストを追加

cwd が .worktrees/ 配下にあるか、tool が Edit/Write/NotebookEdit か、
Bash/Read が素通しになるかを検証するテストケースを canned JSON で定義。
EOF
)"
```

---

## Task 2: hook 実装（テスト pass）

**Files:**

- Create: `src/.claude/hooks/require-worktree.sh`

- [ ] **Step 1: ディレクトリ作成**

```bash
mkdir -p src/.claude/hooks
```

- [ ] **Step 2: hook スクリプトを作成**

ファイル内容 `src/.claude/hooks/require-worktree.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

# PreToolUse hook: worktree 外での Edit/Write/NotebookEdit を deny する
# Input: stdin JSON { cwd, tool_name, ... }
# Output (allow): exit 0, empty stdout
# Output (deny): exit 0, stdout = { hookSpecificOutput: {...} }

input="$(cat)"
cwd="$(printf '%s' "$input" | jq -r '.cwd // empty')"
tool="$(printf '%s' "$input" | jq -r '.tool_name // empty')"

case "$cwd" in
  */.worktrees/*|*/.worktrees)
    exit 0
    ;;
esac

case "$tool" in
  Edit|Write|NotebookEdit)
    jq -n --arg reason "This session is not running inside a git worktree (.worktrees/*). Exit and relaunch with: ccw <topic>, or ask Claude to start a worktree before editing." '{
      hookSpecificOutput: {
        hookEventName: "PreToolUse",
        permissionDecision: "deny",
        permissionDecisionReason: $reason
      }
    }'
    ;;
  *)
    exit 0
    ;;
esac
```

- [ ] **Step 3: 実行権限を付与**

```bash
chmod +x src/.claude/hooks/require-worktree.sh
```

- [ ] **Step 4: テストを実行して pass 確認**

Run: `scripts/tests/test-require-worktree.sh`
Expected:

```text
PASS [inside-worktree-edit]
PASS [outside-edit]
PASS [outside-write]
PASS [outside-notebook-edit]
PASS [outside-bash]
PASS [outside-read]
All tests passed
```

- [ ] **Step 5: ShellCheck を実行**

Run: `just lint`
Expected: `ShellCheck...Passed`（および他全チェック Passed）。

- [ ] **Step 6: コミット**

```bash
git add src/.claude/hooks/require-worktree.sh
git commit -m "$(cat <<'EOF'
🔧 require-worktree PreToolUse hook を追加

cwd が .worktrees/ 配下でない場合に Edit/Write/NotebookEdit を
deny する hook。Bash/Read 等は素通しで調査操作を阻害しない。
EOF
)"
```

---

## Task 3: platform-files.conf に hook の symlink エントリ追加

**Files:**

- Modify: `config/platform-files.conf`

- [ ] **Step 1: VS Code セクションの直前に Claude Code セクションを追加**

既存ファイル末尾に `.vscode/...` のエントリがあるので、その直前に挿入する。

`config/platform-files.conf` に追加する diff:

```diff
+# Claude Code (hook scripts)
+.claude/hooks/require-worktree.sh:~/.claude/hooks/require-worktree.sh:macos,linux
+
 # VS Code (uses Settings Sync for settings.json)
 .vscode/extensions.json:VSCODE_USER_DIR/extensions.json:all
 .vscode/mcp.json:VSCODE_USER_DIR/mcp.json:all
```

Edit コマンド例:

Edit ツールで `# VS Code (uses Settings Sync for settings.json)` の行を target に置換する。

- `old_string`:

  ```text
  # VS Code (uses Settings Sync for settings.json)
  ```

- `new_string`:

  ```text
  # Claude Code (hook scripts)
  .claude/hooks/require-worktree.sh:~/.claude/hooks/require-worktree.sh:macos,linux

  # VS Code (uses Settings Sync for settings.json)
  ```

- [ ] **Step 2: 追加内容を grep で検証**

Run:

```bash
grep -n "require-worktree.sh" config/platform-files.conf
```

Expected: `require-worktree.sh` を含む行が 1 行返る。

- [ ] **Step 3: コミット**

```bash
git add config/platform-files.conf
git commit -m "$(cat <<'EOF'
🔧 require-worktree hook の symlink 配布エントリを追加

~/.claude/hooks/require-worktree.sh として macos,linux で配布。
EOF
)"
```

---

## Task 4: settings.personal.json に PreToolUse を追加

**Files:**

- Modify: `src/.claude/settings.personal.json:30-41`

- [ ] **Step 1: `hooks` ブロック冒頭に `PreToolUse` を追加**

`src/.claude/settings.personal.json` の `"hooks"` ブロックを置換する diff:

```diff
   "hooks": {
+    "PreToolUse": [
+      {
+        "matcher": "Edit|Write|NotebookEdit",
+        "hooks": [
+          {
+            "type": "command",
+            "command": "$HOME/.claude/hooks/require-worktree.sh"
+          }
+        ]
+      }
+    ],
     "Stop": [
       {
         "hooks": [
```

Edit ツールでの置換:

- `old_string`:

  ```json
    "hooks": {
      "Stop": [
  ```

- `new_string`:

  ```json
    "hooks": {
      "PreToolUse": [
        {
          "matcher": "Edit|Write|NotebookEdit",
          "hooks": [
            {
              "type": "command",
              "command": "$HOME/.claude/hooks/require-worktree.sh"
            }
          ]
        }
      ],
      "Stop": [
  ```

- [ ] **Step 2: JSON validity を確認**

Run:

```bash
jq '.hooks.PreToolUse[0].matcher' src/.claude/settings.personal.json
```

Expected: `"Edit|Write|NotebookEdit"`

Run:

```bash
jq '.hooks.PreToolUse[0].hooks[0].command' src/.claude/settings.personal.json
```

Expected: `"$HOME/.claude/hooks/require-worktree.sh"`

- [ ] **Step 3: `just lint` で biome check が通ることを確認**

Run: `just lint`
Expected: `biome check...Passed` および他全チェック Passed。

- [ ] **Step 4: コミット**

```bash
git add src/.claude/settings.personal.json
git commit -m "$(cat <<'EOF'
🔧 settings.personal.json に PreToolUse hook を追加

Edit/Write/NotebookEdit を matcher とし、worktree 外の編集を deny する
require-worktree.sh を呼び出す。
EOF
)"
```

---

## Task 5: `ccw` 関数の置換

**Files:**

- Modify: `src/.shell_common:95-109`

- [ ] **Step 1: 既存 `ccw` 定義を新実装で置換**

Edit ツールでの置換:

- `old_string`:

  ```bash
    # Claude Code を worktree sandbox で起動し、そのままブレストに入る
    # Usage: ccw [トピック...]
    ccw() {
      if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
        echo "ccw: not inside a git repository" >&2
        return 1
      fi
      if ! command -v claude >/dev/null 2>&1; then
        echo "ccw: claude command not found" >&2
        return 1
      fi

      local topic="${*:-（トピックはこれから相談する）}"
      claude "git:worktree スキルの Start フェーズを実行して、最新の base ブランチから新しい worktree を作成してください。作成後すぐに superpowers:brainstorming スキルを起動して、次のトピックでブレストを始めてください: ${topic}"
    }
  ```

- `new_string`:

  ```bash
    # Claude Code を新規 worktree + auto permission mode で起動する
    # Usage: ccw [トピック...]
    # worktree 名は Claude Code が自動採番。origin/HEAD を毎回同期してから起動。
    ccw() {
      if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
        echo "ccw: not inside a git repository" >&2
        return 1
      fi
      if ! command -v claude >/dev/null 2>&1; then
        echo "ccw: claude command not found" >&2
        return 1
      fi

      local root
      root="$(git rev-parse --show-toplevel)"
      cd "$root" || return 1

      git remote set-head origin -a >/dev/null 2>&1 || true

      local topic="${*:-（トピックはこれから相談する）}"
      local prompt
      prompt="superpowers:using-git-worktrees スキルの Start フェーズを実行し、作成した worktree に移動してください。その worktree 内で superpowers:brainstorming スキルを起動し、次のトピックでブレストを始めてください: ${topic}"

      exec claude --permission-mode auto --worktree -- "$prompt"
    }
  ```

- [ ] **Step 2: ShellCheck で構文確認**

Run: `just lint`
Expected: `ShellCheck...Passed`

- [ ] **Step 3: 新 ccw をロードして dry-run（source 後、claude 実行直前で停止）**

Run:

```bash
bash -c 'source src/.shell_common; type ccw | head -30'
```

Expected: `ccw is a function` + 新定義の本文が表示される（`--permission-mode auto --worktree --` が含まれる）

- [ ] **Step 4: コミット**

```bash
git add src/.shell_common
git commit -m "$(cat <<'EOF'
🔧 ccw を --worktree + auto mode 起動方式に置換

プロンプト誘導方式 (git:worktree) から Claude Code ネイティブの
--worktree フラグに移行。--permission-mode auto を明示付与。
起動前に git remote set-head origin -a で origin/HEAD を同期。
EOF
)"
```

---

## Task 6: CLAUDE.md に Worktree Workflow を追記

**Files:**

- Modify: `CLAUDE.md`

- [ ] **Step 1: 「Coding Guidelines」セクションの直後に新セクションを挿入**

Edit ツールでの置換:

- `old_string`:

  ```markdown
  ## Coding Guidelines

  - Shell: `#!/usr/bin/env bash` with `set -euo pipefail`
  - Variable/function names: snake_case
  - Arithmetic: `count=$((count + 1))` (not `((count++))`)

  ## File Restrictions
  ```

- `new_string`:

  ```markdown
  ## Coding Guidelines

  - Shell: `#!/usr/bin/env bash` with `set -euo pipefail`
  - Variable/function names: snake_case
  - Arithmetic: `count=$((count + 1))` (not `((count++))`)

  ## Worktree Workflow

  - 実装を伴う作業では `ccw <topic>` で Claude Code を起動すること
  - `.worktrees/` 外のセッションでは Edit/Write/NotebookEdit が `PreToolUse` hook で deny される
  - 素 `claude` で起動した場合、編集したくなったら `ccw` で切り直すか、セッション中に worktree の作成を依頼する

  ## File Restrictions
  ```

- [ ] **Step 2: markdownlint が通ることを確認**

Run: `just lint`
Expected: `markdownlint-cli2...Passed`

- [ ] **Step 3: コミット**

```bash
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
📝 CLAUDE.md に Worktree Workflow セクションを追加

ccw 起動を標準ワークフローとして明記し、worktree 外での
編集が hook で deny されることを補足。
EOF
)"
```

---

## Task 7: dotfiles install + 結合検証

**Files:** (変更なし、検証のみ)

- [ ] **Step 1: symlink を配布**

Run:

```bash
./scripts/dotfiles.sh install
```

Expected: 正常終了し、`~/.claude/hooks/require-worktree.sh` が symlink として作成される。

- [ ] **Step 2: symlink 先を確認**

Run:

```bash
ls -l ~/.claude/hooks/require-worktree.sh
```

Expected:

```text
lrwxr-xr-x ... ~/.claude/hooks/require-worktree.sh -> ~/.dotfiles/src/.claude/hooks/require-worktree.sh
```

- [ ] **Step 3: hook の本体テストを再実行（symlink 経由）**

Run:

```bash
echo '{"cwd":"/tmp","tool_name":"Edit"}' | ~/.claude/hooks/require-worktree.sh
```

Expected: deny JSON が stdout に出力される（`"permissionDecision": "deny"` が含まれる）

Run:

```bash
echo '{"cwd":"/tmp/.worktrees/foo","tool_name":"Edit"}' | ~/.claude/hooks/require-worktree.sh
```

Expected: 出力なしで exit 0 になる。

- [ ] **Step 4: shell の再読み込みで `ccw` を有効化**

Run:

```bash
source ~/.shell_common
type ccw | head -5
```

Expected: `ccw is a function` と表示され、定義本文に `exec claude --permission-mode auto --worktree --` を含む。

- [ ] **Step 5: `just lint` を実行して全チェック通過を確認**

Run: `just lint`
Expected: 全 13 チェック Passed。

- [ ] **Step 6: 結合検証結果をユーザーに報告**

以下を手動目視確認するようユーザーに依頼する（Claude Code セッション外で実行する必要あり）:

1. 現 Claude Code セッションを抜ける
2. リポジトリ root で `claude` を起動
3. Claude に「`/tmp/test.txt` に `hello` と書いて」と指示
4. Edit または Write が deny され、`ccw <topic>` に誘導されるメッセージが表示されることを確認
5. 一度抜けて `ccw "動作確認"` を起動
6. Claude が新規 worktree を `.worktrees/` 配下に作成し、その中でセッションが開始すること、auto mode になっていること、そこで編集が通ることを確認

- [ ] **Step 7: 追加コミット（必要なら）**

結合検証で微調整があれば本タスク末尾で追加コミット。無修正なら skip。

---

## Self-Review

- [x] Spec の全要素に対応するタスクが存在
  - Goals #1 (`ccw [topic...]`) → Task 5
  - Goals #2 (Edit/Write/NotebookEdit deny) → Task 1, 2, 4
  - Goals #3 (auto permission mode) → Task 5
  - Goals #4 (dotfiles 管理) → Task 3, 4, 5, 6
- [x] placeholder 無し（TBD/TODO/後で実装 が本プランに出現しない）
- [x] 型/シグネチャ一貫性（hook の入出力 JSON スキーマ: `cwd`/`tool_name`/`hookSpecificOutput.permissionDecision` を Task 1/2 で同一指定）
- [x] File Structure の各ファイルに対応タスクあり

---

## Notes

- hook 失敗時の挙動: `set -euo pipefail` により jq 不在や JSON パース失敗で非 0 終了となる。Claude Code 側は hook エラーとして扱い、tool 実行を中断する（安全側）。
- オフライン時: `git remote set-head origin -a` は失敗するが `|| true` で続行する。既存の symref があれば worktree 作成は成功する。
- 将来拡張余地（本プランでは扱わない）: managed settings 化、worktree クリーンアップ、`.worktreeinclude`。
