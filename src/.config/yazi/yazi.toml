# yazi.toml
# https://yazi-rs.github.io/docs/configuration/yazi
#
# Intentionally minimal — defaults work well for the default zellij layout.
# Extend here when preview helpers, openers, or previewers are needed.

[mgr]
show_hidden = true
sort_by = "natural"
sort_dir_first = true
